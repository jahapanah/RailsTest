version: '3'

services:
  app:
    image: jigarsingh/merakitest:tagname
  
    command: bash -c "rm -f tmp/pids/server.pid && bundle exec rails s -p 3000 -b '0.0.0.0' && rspec spec/system/hello_world_spec.rb"
    ports:
      - "3000:3000"
  
